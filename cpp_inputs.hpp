#include <iostream>
#include <limits>

using std::cin;
using std::cout;
using std::string;

double get_double();
uint64_t get_64int();
bool is_decimal_str(string input);
bool is_decimal_str(string input);
bool is_int_str(string input);
